# Tools

향후 quantization, target conversion, Vela 및 artifact verification의 canonical 도구를 둔다. 기능별 버전 suffix가 붙은 script를 만들지 않고 configuration으로 차이를 표현한다.

현재는 Python version과 scaffold 상태만 확인하는 `verify_environment.py`가 있다. 실제 vendor toolchain 검사는 deployment 구현 시 추가한다.
